---
import AuthorCard from '@/components/AuthorCard.astro'
import Breadcrumbs from '@/components/Breadcrumbs.astro'
import PageHead from '@/components/PageHead.astro'
import Layout from '@/layouts/Layout.astro'
import { getAllAuthors } from '@/lib/data-utils'

const authors = await getAllAuthors()
---

<Layout class="max-w-3xl">
  <PageHead slot="head" title="Authors" />
  <Breadcrumbs items={[{ label: 'Authors', icon: 'lucide:users' }]} />
  {
    authors.length > 0 ? (
      <ul class="flex flex-col gap-4">
        {authors.map((author) => (
          <li>
            <AuthorCard author={author} />
          </li>
          <p>
            As a kid raised by internet forums, I was obsessed with customizing my signatures and “about me” sections on personal profiles using BBCode (you can add [img]cool-images.png[/img] and [b]bold text[/b]!). I maximized personality and expression through these gradient generator tools to add awesome colors, these weird cursive 𝒰𝓃𝒾𝒸𝑜𝒹𝑒 𝒸𝒽𝒶𝓇𝒶𝒸𝓉𝑒𝓇𝓈, or perfectly dimensioned Ezgif GIF art. To this day, the web still remains my favorite design medium!
          </p>
        ))}
      </ul>
    ) : (
      <p class="text-muted-foreground text-center">No authors found.</p>
    )
  }
</Layout>
